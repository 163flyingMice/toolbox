# tool

Cloudbase AI Builder project.

## 本地预览

```bash
npm install
npm run dev
```

默认地址：

```text
http://127.0.0.1:5173/
```

本地预览会用 Vite 模拟 CloudBase 的 `$w` 路由能力，并把 `/api/stock-quotes` 转发到 `cloudfunctions/stock-quotes`。
